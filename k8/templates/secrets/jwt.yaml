apiVersion: v1
kind: Secret
metadata:
  name: jwt
type: Opaque
data:
{{- range $key, $value := .Values.secret.jwt }}
{{- if $value }}
{{- if eq (typeOf $value) "string" }}
  {{ $key }}: {{ $value | toString | b64enc }}
{{- end }}
{{- end }}
{{- end }}
