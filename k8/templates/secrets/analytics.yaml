apiVersion: v1
kind: Secret
metadata:
  name: analytics
type: Opaque
data:
{{- range $key, $value := .Values.secret.analytics }}
{{- if $value }}
{{- if eq (typeOf $value) "string" }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
{{- end }}
{{- end }}

