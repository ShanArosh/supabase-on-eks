apiVersion: v1
kind: Secret
metadata:
  name: dashboard
type: Opaque
data:
{{- range $key, $value := .Values.secret.dashboard }}
{{- if $value }}
{{- if eq (typeOf $value) "string" }}
  {{ $key }}: {{ $value | b64enc }}
{{- end }}
{{- end }}
{{- end }}
